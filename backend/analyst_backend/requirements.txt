--find-links ./wheels
ai-firewall==0.2.1

# ── Web framework ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9
# asyncpg — async Postgres driver used by strategist/db/connection.py and
# repositories.py. Required alongside psycopg2-binary because the strategist
# code path uses async/await; psycopg2 is sync-only and can't be awaited.
asyncpg>=0.29.0

# ── Cache / queue ──────────────────────────────────────────────────────────────
redis==5.0.4

# ── Auth ───────────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4   # legacy/unused for passwords now (kept for compat)
bcrypt>=4.0             # app/security.py hashes passwords with bcrypt directly

# ── File uploads ───────────────────────────────────────────────────────────────
python-multipart==0.0.9

# ── Config ─────────────────────────────────────────────────────────────────────
pydantic-settings==2.2.1
python-dotenv==1.0.1

# ── Data ───────────────────────────────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
openpyxl==3.1.2

# ── HTTP client (for agent calls) ──────────────────────────────────────────────
httpx==0.27.0

# ── ML / Churn Prediction ─────────────────────────────────────────────────────
scikit-learn==1.5.0
xgboost==2.0.3
joblib==1.4.2
# shap — required for per-customer driver attribution when the AUC winner is
# NOT XGBoost (Random Forest, Logistic Regression). XGBoost ships TreeSHAP
# built in; sklearn models don't, so predict.compute_churn_drivers falls back
# to this package. Without it the fallback path raises ImportError and all
# driver_1/2/3 columns are written as NULL, making the Churn Scores table
# show "—" for the Top Driver column.
shap>=0.44.0

# ── Sentiment Analysis ────────────────────────────────────────────────────────
vaderSentiment==3.3.2

# ── LLM + Agent (Groq for outreach emails, LangGraph for Agent Chat) ─────────
langchain-groq==1.1.2
# langchain-core==1.2.28
langchain-core==1.2.31
langgraph>=0.2.0

# ── Web scraping (Scout Agent) ────────────────────────────────────────────────
playwright==1.58.0
playwright-stealth==2.0.3
lxml
beautifulsoup4

# ── Observability / Cost Tracking ─────────────────────────────────────────────
# LangFuse v2 SDK — we use it directly (langfuse.Langfuse().trace().generation())
# NOT via the LangChain CallbackHandler, because that handler imports
# langchain_core.pydantic_v1 which was removed in langchain-core 1.x and
# crashed the agent mid-invocation. Direct SDK is stable across langchain versions.
# Pinning <3.0.0 because v3 restructured the module layout.
langfuse>=2.40.0,<3.0.0
langchain-openai==1.1.14
openai==2.32.0